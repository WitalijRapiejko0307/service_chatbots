# Agent configs

YAML/JSON agent configs will live here in later blocks.

Domain: generic service businesses (salon, school, shop, support, FAQ, leads) — not medical.

Example filename (create when an agent is first exported):

```
# service_agent_001.yaml
```

Do not store API tokens in these files. Channel tokens belong in gitignored env / the bindings table.
